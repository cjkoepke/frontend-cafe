import Head from 'next/head';

const HeadMeta = () => (
    <Head>
        <title>Frontend.Cafe - A community for frontend developers and software engineers.</title>
        <meta name="viewport" content="initial-scale=1.0, width=device-width" />
    </Head>
)

export default HeadMeta;
